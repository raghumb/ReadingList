# Agent Patterns

[Agent Patterns](https://cloud.google.com/architecture/choose-design-pattern-agentic-ai-system)

- Single-agent system
- Multi-agent systems
    a. Sequential pattern
    b. Parallel pattern (resource cost, token cost, complexity, reduced latency)
    c. Loop pattern (repeat steps until exit criteria met). Eg, self correction, iterative refinement
        i. Review and critique pattern
        ii. Iterative refinement pattern
    d. Coordinator pattern (resource cost, token cost, complexity, high latency)
    e. Hierarchical task decomposition pattern
    f. Swarm pattern
    g. Reason and act (ReAct) pattern (think, act, observe. Exit when finds answer or max iterations). (Accuracy depends on quality of reasoning, errors can propagate through steps)
Human-in-the-loop pattern
    h. Human-in-the-loop pattern


